/// <reference types="cypress"/>

import '../../support/commandsContas'

describe('Should test at an interface level', () => {

    let token

    before( () => {
        
    })

    beforeEach(() => {
    })


})